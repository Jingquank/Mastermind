node_modules/
dist/
*.log
.DS_Store

# Mastermind's own per-directory workspace (history snapshots, translation cache)
.mastermind/

# brainstorming visual companion artifacts
.superpowers/

# impeccable live-mode local state (config + session journals)
.impeccable/

# portable design-skill installs (impeccable) — tooling, not product
.agents/
.cursor/
.gemini/

# scratch demo docs generated while dogfooding (the skill regenerates these)
/dummy-demo.md
/mastermind-demo.md
/roadmap-demo.md
