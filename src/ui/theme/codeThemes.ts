// Code-color schemes moved to src/shared/codeThemes.ts (shared so the CLI can list
// them for the setup picker). Re-exported here for existing `theme/codeThemes`
// importers (ThemeProvider, SettingsPanel).
export * from '../../shared/codeThemes'
